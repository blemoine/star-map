import * as React from 'react';
import * as ReactDOM from 'react-dom';
import { AppStateContainer } from './app/AppStateContainer';

ReactDOM.render(<AppStateContainer />, document.getElementById('react-root'));
