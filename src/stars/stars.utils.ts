/*
import { Kelvin } from '../measures/kelvin';

export function computeRadius(temperature: Kelvin, absoluteMagnitude: number) {
  return  SunRadius * Math.pow( SunTemp / temperature , 2) * Math.sqrt(Math.pow(2.512, absoluteMagnitudeSun - absoluteMagnitude))
}
*/
